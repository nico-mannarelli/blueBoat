# Survey contacts, written by each replay_xtf.py / main.py run.
# Committed empty so `from contacts import coords` always works, even before
# the first scan. Each run overwrites the list below.
coords = []
